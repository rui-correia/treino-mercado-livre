micronaut:
  application:
    name: treinoMercadoLivre
datasources:
  default:
    url: jdbc:postgresql://localhost:5433/treinomldb
    driverClassName: org.postgresql.Driver
    username: postgres
    password: postgres

jpa:
  default:
    properties:
      hibernate:
        hbm2ddl:
          auto: update
        show_sql: true